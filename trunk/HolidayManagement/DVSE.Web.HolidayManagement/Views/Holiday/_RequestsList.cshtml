@model DVSE.Web.HolidayManagement.Models.RequestsListViewModel

@using GridMvc.Html

@Html.Grid(Model.Requests).Columns(columns => 
    {
        columns.Add(requestVM => requestVM.StartDate).Titled("Start date");
        columns.Add(requestVM => requestVM.EndDate).Titled("End date");
    })

